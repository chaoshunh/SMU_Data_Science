# INSTALLING AND LOADING PACKAGES IN R

# install with install.packages()
# install from CRAN (comprehensive R Archive Network)
install.packages('ggplot2')

library(ggplot2)

# make sure to use a command from a certain library:

ggplot2::qplot()

